
<% if @latest_backlog.nil? %>

Nothing to show yet, I have no data

<% else %>

Images waiting to send: <strong><%= @latest_backlog.waiting %></strong><br>
Images already sent: <strong><%= @latest_backlog.completed %></strong><br>
Info as of
	<strong>
		<%= timestamp_in_words(@latest_backlog.event.timestamp) %>
	</strong>
<br>

<% end %>
