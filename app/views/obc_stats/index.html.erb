
<h1>Backlog Stats</h1>

<% if @latest_backlog.nil? %>

Nothing to show yet, I have no data

<% else %>

Images waiting to send:
	<strong>
		<%= @latest_backlog.waiting_count %>
		(<%= number_to_human_size @latest_backlog.waiting_size %>)
	</strong>
<br>
Images already sent:
	<strong>
		<%= @latest_backlog.completed_count %>
		(<%= number_to_human_size @latest_backlog.completed_size %>)
	</strong>
<br>
Info as of
	<strong>
		<%= timestamp_in_words(@latest_backlog.event.timestamp) %>
	</strong>
<br>

<!-- Device tyime: x, received x -->

<% end %>

<h1>Latest Transfer Info</h1>

<% if @latest_transfers.empty? %>

Nothing to show yet, I have no data

<% else %>

<% @latest_transfers.each do |t| %>
<p>
File: <strong><%= t.name %></strong><br>
Size: <strong><%= number_to_human_size(t.size) %></strong><br>
Speed: <strong><%= speed_in_words(t.size, t.duration) %></strong>
</p>
<% end %>

<% end %>
